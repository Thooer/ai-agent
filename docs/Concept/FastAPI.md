FastAPI

现代、高性能的 Python Web 框架，基于 ASGI 标准构建，专为构建 API 而设计。它自动生成 OpenAPI 文档，支持异步处理、类型提示校验（Pydantic），在 AI Agent 开发中常用于快速搭建后端服务，让 Agent 能够通过 HTTP 接口暴露能力、接收用户请求或与其他系统交互。

大白话：FastAPI 就是用来快速写 API 的 Python 工具，写得好还自动出文档。
