pgvector

pgvector 是 PostgreSQL 的一个扩展插件，让传统关系型数据库具备向量存储和相似度检索能力。好处是不用引入新的数据库组件，业务数据和向量数据放在同一个 PostgreSQL 里，SQL 查询、事务、权限管理全都现成，更贴近企业级项目的技术选型。

大白话：pgvector 就是给 PostgreSQL 加了个向量搜索功能，不用多装一个数据库，企业项目里最实用。
